# Inventory-Application

Open the folder WarehouseInventoryApp on Android Studio and build with the necessary gradle plugins.

This application utilises advanced UI elements like appbar, drawer, toasts, snackbar, ListView, RecyclerView, CardViews etc. and automatically takes content from SMS for input into the application. It also uses SQLite and RoomDatabase to persist data locally.
